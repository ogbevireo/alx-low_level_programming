#include "main.h"
/**
*_abs - Calculate the absolute value of an integer
*
*Return: The absolute value of the integer or (i * -1)
*
* @i: Integer character
*/

int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
