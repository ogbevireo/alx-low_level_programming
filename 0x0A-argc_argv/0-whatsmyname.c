#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the name of the programme called even when renamed
 * @argc: argument count
 * @argv: argument parameter
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
